akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"

  debug {
    receive = off
    unhandled = off
  }

  actor.provider = "akka.cluster.ClusterActorRefProvider"

  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
      hostname = "127.0.0.1"
      port = 2551
    }
  }

  cluster {
    roles = ["web"]
    seed-nodes = [
      "akka.tcp://UploadService@127.0.0.1:2551",
      "akka.tcp://UploadService@127.0.0.1:2552"]
  }
}

spray.can {

  server {
    pipelining-limit = 5

    verbose-error-logging = on
  }

  host-connector {
    pipelining = on
    max-connections = 10
  }
}

c3p0 {
  jdbcUrl = "jdbc:postgresql://10.0.10.166:6432/tst_ts?stringtype=unspecified&user=webedi&password=123456"
  driverClass = "org.postgresql.Driver"
  minPoolSize = 5
  maxPoolSize = 20
  acquireIncrement = 5
}

infotecs {
  service {
    bind {
      host = "127.0.0.1"
      port = "10000"
    }
  }

  file-server {
    host = "10.0.10.165"
    port = "80"
    uploadUrl = "/upload"
  }

  operator {
    fnsId = "2AH"
    company = "ОАО \"Инфотекс Интернет Траст\""
    inn = "7743020560"
    kpp = "771401001"
  }
}